
int isCharRep(char character);
int isStrRep(char* str);
int isCharacter(char state);
int isString(char* states);
void print1inARow();
void print2inARow();

typedef struct
{
	char* str;
}String;

typedef struct
{
	char* states;
}States;